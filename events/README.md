# events

Event-shaped Inn Ambush history records live here when they are split from the bundled Plan B authority journal.

The first mesh seed keeps `CommittedEvent` and `StateMutation` examples bundled in `authority/journals/plan-b/plan-b.ttl` and cross-referenced from `world/states/inn-ambush-plan-b-state/inn-ambush-plan-b-state.ttl`.
